(function() {

  var app = angular.module("YW-Editor-WebApp", []);

  var MainController = function($scope, $http) {
  };

  app.controller("MainController", ["$scope", "$http", MainController]);

}());
